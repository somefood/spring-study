<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="me.seokju.mybatisstudy.dao.MemberDao">

    <!-- 뉴스 조회 -->
    <select id="findAll" resultType="me.seokju.mybatisstudy.vo.Member">
        select member_id, name from member
    </select>
</mapper>